import { TransformScaleDirective } from './transform-scale.directive';

describe('TransformScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformScaleDirective();
    expect(directive).toBeTruthy();
  });
});
